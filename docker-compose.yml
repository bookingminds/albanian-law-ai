version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist database and uploads across restarts
      - app_data:/app/data
      - app_uploads:/app/uploads
    restart: unless-stopped

volumes:
  app_data:
  app_uploads:
